import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// no DOM (Elementos HTML).render(<Componente(função que retorna HTML) />, no documento com ID 'root')
ReactDOM.render(<App />,  document.getElementById('root'));

